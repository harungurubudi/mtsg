basePath: /
definitions:
  internal_presentation_http_handler.PingResponse:
    properties:
      message:
        example: pong
        type: string
      status:
        example: ok
        type: string
      timestamp:
        example: "2023-07-29T17:30:00Z"
        type: string
    type: object
host: localhost:8080
info:
  contact:
    email: support@swagger.io
    name: API Support
    url: http://www.swagger.io/support
  description: Multi-Tenant SaaS Gateway API
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  termsOfService: http://swagger.io/terms/
  title: MTSG API
  version: "1.0"
paths:
  /ping:
    get:
      consumes:
      - application/json
      description: Simple ping endpoint to check if the server is running
      produces:
      - application/json
      responses:
        "200":
          description: Server is running
          schema:
            $ref: '#/definitions/internal_presentation_http_handler.PingResponse'
      summary: Health check endpoint
      tags:
      - Health
securityDefinitions:
  BearerAuth:
    description: Type "Bearer" followed by a space and JWT token.
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
tags:
- description: Health check operations
  name: Health
- description: Authentication operations
  name: Authentication
- description: User management operations
  name: Users
- description: Tenant management operations
  name: Tenants
