# Authentication Package Rule

This rule defines the structure and requirements for the authentication package, including credential and session entities.

description: Defines authentication domain entities, error types, validation rules, and best practices for secure user authentication.
globs:
alwaysApply: false

## Purpose
- Provide a standard for user authentication input (credentials) and output (session tokens)
- Ensure type safety, validation, and reusability across the authentication flow
- Support multi-tenant authentication with proper tenant isolation

## Token Subject Constants
- **AccessTokenSubject**: Constant string value representing the subject for access tokens (e.g., "access_token").
- **RefreshTokenSubject**: Constant string value representing the subject for refresh tokens (e.g., "refresh_token").
- **Usage**: These constants should be used as the `Subject` field in token claims to distinguish between access and refresh tokens throughout the authentication flow.

## Errors
- **InvalidCredential**: Returned when login fails for any reason (invalid email, password, or user not found). This error should not reveal which part of the credential was incorrect, for security reasons.
- **UserNotFound**: Returned when the user with the provided email does not exist.
- **AccountLocked**: Returned when the user account is locked or suspended.
- **PasswordExpired**: Returned when the user's password has expired and needs to be changed.
- **UserInactive**: Returned when the user account is inactive.

## Entities

### Credential
Represents the input required for user authentication (login).

- **Fields:**
  - Email: type `user.Email` (referenced from user package), validated as `required,email`
  - Password: type `user.Password` (referenced from user package), validated as `required,password`
- **Best Practices:**
  - Do not store credentials; use only for authentication input
  - Always validate both fields before processing
- **Method:**
  - `Verify(ctx context.Context, matchedUser *user.User) error`: Checks if the provided password matches the user's stored ciphertext. Returns nil if matched, otherwise returns InvalidCredential. Uses the Password's validation and matching logic.
- **Example Implementation:**
```go
func (c *Credential) Verify(ctx context.Context, matchedUser *user.User) error {
    if !c.Password.Validate() {
        return InvalidCredential
    }
    if !matchedUser.CipherText.Matches(c.Password) {
        return InvalidCredential
    }
    return nil
}
```

### Session
Represents the output of a successful authentication (login), containing issued tokens.

- **Fields:**
  - AccessToken: type `token.Token` (referenced from token package)
  - RefreshToken: type `token.Token` (referenced from token package)
- **Best Practices:**
  - AccessToken is used for short-lived API access
  - RefreshToken is used to obtain new access tokens
  - Consider implementing refresh token rotation for enhanced security
  - Do not expose sensitive token details in logs or user-facing messages

## Validation
- Credential fields must be validated using struct tags and/or validation logic
- Session tokens must be generated and validated according to the token package rules
- UserContext should be validated to ensure all required fields are present

## Example Usage
```go
const (
    AccessTokenSubject  = "access_token"
    RefreshTokenSubject = "refresh_token"
)

// Error types for specific authentication scenarios
var (
    InvalidCredential = errors.New("invalid credentials")
    UserNotFound      = errors.New("user not found")
    AccountLocked     = errors.New("account is locked")
    UserInactive      = errors.New("user account is inactive")
)

// Credential input for login
type Credential struct {
    Email    user.Email    `json:"email" validate:"required,email"`
    Password user.Password `json:"password" validate:"required,password"`
}

// Session output after successful login
type Session struct {
    AccessToken  token.Token `json:"access_token"`
    RefreshToken token.Token `json:"refresh_token"`
}
```

## Best Practices
- Keep authentication logic separate from domain logic
- Use dependency injection for token and user services
- Ensure all sensitive data is handled securely and never logged

---

Note: This rule ensures a consistent, secure, and maintainable approach to authentication input and output across the project. For token generation and validation, reference the token package rules.
---